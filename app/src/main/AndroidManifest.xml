<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.loopcom.ccoderad.biliinformation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AnimeInfo"
            android:label="@string/search_new_old" />
        <activity
            android:name=".WeekRank"
            android:label="每周推荐" />
        <activity
            android:name=".detiails_rank"
            android:label="@string/title_activity_detiails_rank"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".live_list"
            android:label="精选直播"
            ></activity>
    </application>

</manifest>
